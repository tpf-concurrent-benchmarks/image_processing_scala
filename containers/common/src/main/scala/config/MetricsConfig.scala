package org.image_processing.common
package config

case class MetricsConfig(
                            host: String,
                            port: Int,
                            prefix: String
                        )
