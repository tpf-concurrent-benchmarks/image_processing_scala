package org.image_processing.common
package config

case class QueuesConfig(
                           work: String,
                           results: String,
                           endEvent: String
                       )
