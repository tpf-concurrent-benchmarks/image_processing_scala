middleware {
    host: "rabbitmq"
    port: 5672
    user: "guest"
    password: "guest"
    endEvent: "end"
    queues {
        input: "formatting"
        output: "scaling"
    }
}
metrics {
    host: "graphite"
    port: 8125
    prefix: "required"
}